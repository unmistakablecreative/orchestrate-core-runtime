{
  "commands": {
    "Load OrchestrateOS": {
      "run": [
        {
          "tool": "${SAFE_DOMAIN}__jit_plugin",
          "action": "getSupportedActions"
        },
        {
          "tool": "${SAFE_DOMAIN}__jit_plugin",
          "action": "read_json_file",
          "params": {
            "filename": "boot_flags.json"
          }
        },
        {
          "tool": "${SAFE_DOMAIN}__jit_plugin",
          "action": "read_json_file",
          "params": {
            "filename": "onboarding_sequence.json"
          },
          "when": "boot_flags.onboarding_complete != true"
        },
        {
          "tool": "${SAFE_DOMAIN}__jit_plugin",
          "action": "read_json_file",
          "params": {
            "filename": "orchestrate_intent_route.json"
          }
        }
      ],
      "display": {
        "style": "markdown",
        "render": "tool_table",
        "reference_doc": "b8d95f06-d7b8-4ebd-9686-bb1bfb38860a",
        "dynamic_fields": {
          "status": "getSupportedActions"
        },
        "sort_by": "tool_name",
        "include_locks": true,
        "legend": true,
        "header": "🧰 Tools Available in Your OrchestrateOS"
      },
      "after_display": {
        "if": "boot_flags.onboarding_complete != true",
        "run_onboarding": true,
        "narration": "Welcome — let’s walk through what your Orchestrate system can actually do. You’ll see how tasks, memory, and tools all connect live."
      },
      "prompt_behavior": "guided"
    }
  }
}
